#include <stdio.h>
/**
 * main - function
 * @argc: number of arguments
 * @argv: array of pointers
 * Return: 0
 */
int main(int argc, char __attribute__ ((unused)) *argv[])
{
	if (argc >= 0)
	{
		printf("%d\n", argc);
	}
	return (0);
}
