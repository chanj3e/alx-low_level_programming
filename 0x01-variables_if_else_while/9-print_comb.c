#include <stdio.h>
/**
 * main - prog that prints all possible combinations of single digit
 * Return: 0 Always (Success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
	{
		putchar(i);
		if (i != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
